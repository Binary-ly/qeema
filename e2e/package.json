{
  "name": "qeema-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for the Qeema reporter PWA and public surface",
  "license": "Apache-2.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
